<h1>{{campground.name}}</h1>
<img src="{{campground.image}}">
<p>{{campground.description}}</p>
{{#each campground.comments}}
    <p><strong>{{author}}</strong> - {{text}}</p>
{{/each}}
<a href="{{campground._id}}/comments/new">add comment</a>